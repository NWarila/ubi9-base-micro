# Purpose: Deny-all Docker build-context filter; allowlist only build inputs.
# Role: config
# Micro-container candidate: no (build-context filter, not gate logic)
# Note: place header above the leading `**` deny rule.

**
!containers/
!containers/Dockerfile
!containers/fips/
!containers/fips/openssl.cnf
!tools/
!tools/assert-builder-toolchain-floor.sh
!tools/assert-rpm-lock-hashes.sh
!tools/build-runtime-rootfs.py
!tools/fetch-builder-rpms.sh
!tools/fetch-openssl-fips-provider-rpms.sh
!tools/fetch-runtime-rpms.sh
!tools/rpmlock.py
!rpm-lock/
!rpm-lock/*.txt
